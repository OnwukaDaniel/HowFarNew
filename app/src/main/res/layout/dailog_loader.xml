<?xml version="1.0" encoding="utf-8"?><!-- code created by RetryTech / Skype : RetryTech / Mail = tejash@gmail.com -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_10dp"
    android:backgroundTint="#A9000000"
    android:padding="30dp">

    <ImageView
        android:id="@+id/iv_parent"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_centerInParent="true"
        android:src="@drawable/bubble_single"
        app:tint="@color/pink" />

    <ImageView
        android:id="@+id/iv_child"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_centerInParent="true"
        android:src="@drawable/bubble_single_small"
        app:tint="@color/purple" />
</RelativeLayout>