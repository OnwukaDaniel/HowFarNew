<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#A8121212"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center"
    tools:context=".dilog.DialogMessageFragment">

    <androidx.cardview.widget.CardView
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:layout_margin="38dp"
        app:cardBackgroundColor="#2B2B2B"
        app:cardCornerRadius="24dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:id="@+id/dialog_msg"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_margin="15dp"
                android:gravity="center"
                android:text="Please wait..."
                android:textColor="#ffff"
                android:textSize="20sp" />

            <ProgressBar
                android:id="@+id/determinate_progress_bar_progress"
                style="@android:style/Widget.Material.Light.ProgressBar.Horizontal"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginHorizontal="52dp"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="12dp"
                android:contentDescription="download progress"
                android:indeterminate="false"
                android:progress="0"
                app:indicatorColor="@color/white"
                app:trackColor="@color/batli"
                app:trackCornerRadius="10dp" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>